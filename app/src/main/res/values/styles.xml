<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorhomelan</item>
        <item name="colorPrimaryDark">@color/colorhomelan</item>
        <item name="colorAccent">@color/colorhomelan</item>

        <item name="actionBarStyle">@style/MyActionBar</item>
    </style>
    <!--Customer actionBarStyle-->
    <style name="MyActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="titleTextStyle">@style/ActionBar.TitleText</item>
        <item name="background">@color/colorhomelan</item>
        <item name="actionBarItemBackground">@color/colorhomelan</item>
    </style>

    <!--Custome actionBarStyle TitleText-->
    <style name="ActionBar.TitleText" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:textColor">#fff</item>
        <item name="android:textSize">16sp</item>
    </style>


    <style name="Theme.AppStartLoadTranslucent" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
    </style>

</resources>
